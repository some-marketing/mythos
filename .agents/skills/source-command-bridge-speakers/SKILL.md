---
name: source-command-bridge-speakers
description: "Bridge multiple audio output devices (typically Bluetooth speakers) into a single system output so they play simultaneously"
---

# /bridge-speakers

Canonical authority: `instructions/canonical/commands/bridge-speakers.yaml`. Read that file at execution time; this projection never copies or overrides its behavioral body.

Capability tier: **ADVISORY**.

Read the canonical command at execution time and carry out its workflow with Codex capabilities. This projection is ADVISORY; availability of this skill is not a blocking runtime mechanism.
