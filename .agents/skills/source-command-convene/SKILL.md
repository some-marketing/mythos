---
name: source-command-convene
description: "Convene a triadic Mythos profile on a task. Uses the invoking harness or slot as origin, fires scoped prompts to the other triad slots in parallel, collects responses, and writes artifacts so the origin slot can synthesize across all three voices."
---

# /convene

Canonical authority: `instructions/canonical/commands/convene.yaml`. Read that file at execution time; this projection never copies or overrides its behavioral body.

Capability tier: **ADVISORY**.

Read the canonical command at execution time and carry out its workflow with Codex capabilities. This projection is ADVISORY; availability of this skill is not a blocking runtime mechanism.
