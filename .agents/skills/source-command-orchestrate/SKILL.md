---
name: source-command-orchestrate
description: "Fresh-session handoff wrapper — resolve a plan-id and invoke the orchestrate skill with a synthesized task description"
---

# /orchestrate

Canonical authority: `instructions/canonical/commands/orchestrate.yaml`. Read that file at execution time; this projection never copies or overrides its behavioral body.

Capability tier: **ADVISORY**.

Read the canonical command at execution time and carry out its workflow with Codex capabilities. This projection is ADVISORY; availability of this skill is not a blocking runtime mechanism.
