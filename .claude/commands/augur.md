---
description: Cross-alias (D&D canon) for /site
allowed-tools: [Read, Glob, Grep]
---

<objective>
Run `/site`. `augur` is a cross-alias (augur the right path); the full command body lives under the primary mythic name.
</objective>

<process>
1. Follow `.claude/commands/site.md`.
</process>

<success_criteria>
- `/augur` resolves to the same behavior as `/site`
</success_criteria>
