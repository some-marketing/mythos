---
description: Cross-alias (D&D canon) for /open-contract
allowed-tools: [Read, Write, Glob, Task]
---

<objective>
Run `/open-contract`. `campaign` is a cross-alias (a D&D campaign is a project); the full command body lives under the primary mythic name.
</objective>

<process>
1. Follow `.claude/commands/open-contract.md`.
</process>

<success_criteria>
- `/campaign` resolves to the same behavior as `/open-contract`
</success_criteria>
