---
description: Cross-alias (long form) for /site
allowed-tools: [Read, Glob, Grep]
---

<objective>
Run `/site`. `consult-oracle` is the long-form cross-alias; the full command body lives under the primary mythic name.
</objective>

<process>
1. Follow `.claude/commands/site.md`.
</process>

<success_criteria>
- `/consult-oracle` resolves to the same behavior as `/site`
</success_criteria>
