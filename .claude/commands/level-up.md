---
description: Cross-alias (D&D canon) for /rank-up
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Agent]
---

<objective>
Run `/rank-up`. `level-up` is a cross-alias; the full command body lives under the primary mythic name.
</objective>

<process>
1. Follow `.claude/commands/rank-up.md`.
</process>

<success_criteria>
- `/level-up` resolves to the same behavior as `/rank-up`
</success_criteria>
