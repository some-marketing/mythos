---
description: Compatibility alias for /conclave
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Task]
---

<objective>
Run `/conclave`. `oc` is a compatibility alias; the full command body lives under the primary mythic name.
</objective>

<process>
1. Follow `.claude/commands/conclave.md`.
</process>

<success_criteria>
- `/oc` resolves to the same behavior as `/conclave`
</success_criteria>
