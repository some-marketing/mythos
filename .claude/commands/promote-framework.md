---
description: Plain-id alias for /rank-up
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Agent]
---

<objective>
Run `/rank-up`. `promote-framework` is the plain-software id; the full command body lives under the mythic name.
</objective>

<process>
1. Follow `.claude/commands/rank-up.md`.
</process>

<success_criteria>
- `/promote-framework` resolves to the same behavior as `/rank-up`
</success_criteria>
