---
description: Cross-alias (D&D canon) for /bookshelf
allowed-tools: [Read, Glob, Grep]
---

<objective>
Run `/bookshelf`. `spellbook` is a cross-alias; the full command body lives under the primary mythic name.
</objective>

<process>
1. Follow `.claude/commands/bookshelf.md`.
</process>

<success_criteria>
- `/spellbook` resolves to the same behavior as `/bookshelf`
</success_criteria>
