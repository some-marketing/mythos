---
description: Short form of /ticktock
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Task]
---

<!-- Generated by tools/instructions/generate-alias-stubs.cjs from the
     `tt` entry in instructions/canonical/command-aliases.yaml. The registry entry is the
     authority; do not hand-edit this stub — change the registry and
     regenerate. -->

<objective>
Run `/ticktock`. `tt` is the short form; the full command body lives under `.claude/commands/ticktock.md`.
</objective>

<process>
1. Follow `.claude/commands/ticktock.md`.
</process>

<success_criteria>
- `/tt` resolves to the same behavior as `/ticktock`
</success_criteria>
