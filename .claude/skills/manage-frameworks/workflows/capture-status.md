# Capture Status Workflow

## Steps

1. **[AUTO] Read capture metadata** — Load `CAPTURE_META.json`.
2. **[AUTO] Inspect capture files** — Check required files, imported evidence count, step count, and success criteria.
3. **[AUTO] Report readiness** — Show whether the capture is scaffold-ready and list missing items or weak spots.

## Output

- Capture status summary
- Missing required fields, if any
