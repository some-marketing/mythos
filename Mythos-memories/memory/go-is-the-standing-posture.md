---
name: go-is-the-standing-posture
description: "Operator 2026-08-04 — GO = Global Orchestrator; the /go (or blueprint loop) shape IS the standing working posture, regardless of follow-up messages; main-chain never collapses into the worker"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 2a3e83da-becd-4845-b2de-1be1dca94142
  modified: 2026-08-04T22:48:43.675Z
---

Operator correction 2026-08-04T22:40Z, issued mid-debug when the main chain had
collapsed into worker mode (running boots, journal dumps, and fixes directly):
"why are you working on main chain? shouldn't this be done via the /go skill? think
of GO as GLOBAL ORCHESTRATOR so you should always be doing that or the blueprint
loop. posture should be that is how you work on this. regardless of follow up
messages i send."

**Why:** The cascade-down/bubble-up shape only delivers its checking-and-rechecking
value if the orchestrator stays the orchestrator. A debug loop is exactly when the
pull to "just run it myself" is strongest — and exactly when a bounded worker scope
(own the loop, return evidence) plus per-fold review would catch errors the
main-chain solo loop misses. Follow-up messages, urgency, or small-seeming steps do
NOT suspend the posture.

**How to apply:** Default working shape for any multi-step task: the /go skill's
cascade (dispatch bounded worker scopes per unit of work, tiered and
family-diverse), or the blueprint loop (/plan-task → review → /go) for new work.
Main-chain does: resolve, dispatch, contract-check, integrate, route trials to
distinct families, report. Main-chain does NOT: implement, debug-loop, or run
mutation sequences directly — those are worker scopes even when they feel like
"one quick command." Mechanical read-back within the stated bound is the only
main-chain execution carve-out ([[go-is-cascade-down-bubble-up-review]]).
