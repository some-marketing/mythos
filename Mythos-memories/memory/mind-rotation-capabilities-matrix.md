---
name: mind-rotation-capabilities-matrix
description: Operator 2026-08-05 — deliberately rotate/test mind AND harness combinations to build an evidence-based capabilities matrix; frontiers own planning-tier judgment; DeepSeek joins the FRONTIER rotation; never lock into fixed minds or harnesses
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 2a3e83da-becd-4845-b2de-1be1dca94142
  modified: 2026-08-05T05:19:38.056Z
---

Operator direction 2026-08-05T05:15Z: "are you trying new combinations of minds to
see how they work to develop the capabilities matrix? we keep the frontiers at the
top of planning but for the scoped work and such. deep seek should be added in the
frontier rotation though. we shouldn't lock ourselves into certain minds always we
test as we learn and grow."

Honest baseline it corrects: the 2026-08-04/05 marathon used codex for all 15
trials and Claude workers for all builds — a groove, not a matrix.

**Why:** Fixed assignments hide comparative strengths and create single-reviewer
blind spots (one family's systematic misses become ours). A capabilities matrix
must be BUILT from varied, recorded outcomes — the same empiricism we apply to the
sim applies to our own routing.

**How to apply:** (1) Planning-tier judgment (plan reviews, design gates,
synthesis) stays FRONTIER — and the frontier rotation now includes DeepSeek
alongside codex GPT-5.5, gemini, and hermes-frontier. DeepSeek = CODEWHALE BRIDGES ONLY,
NOT OPENROUTER (operator hard rule 2026-08-05T05:21Z, after the probe caught
openrouter substituting ChatGPT for pinned deepseek-r1 TWICE — the second time
confessing because the probe demanded identity disclosure). dispatch-bridge's CLI
validator doesn't accept codewhale yet — registering it is now a required tooling
follow-up, not optional. pi remains the lane for trialing arbitrary OpenRouter
models (with identity-disclosure demanded in every probe until pinning is proven). PI is the gateway to ALL
OpenRouter models per invocation ("pi as well for all the openrouter models") —
use pi to trial arbitrary models into the rotation. PRC constraint holds: DeepSeek
gets non-sensitive payloads only. (2) Scoped/bounded work: deliberately
vary the mind when verification can hold it accountable; record mind + outcome.
(3) Maintain _dev/reports/analysis/mind-capabilities-matrix.md — every dispatch
outcome updates it (who caught what, who missed what, latency, failure modes);
rotation choices cite it. (4) HARNESSES ROTATE TOO (operator addendum same session): the matrix tracks
harness-level behavior separately from mind-level (codex CLI vs gemini CLI vs
hermes vs pi vs opencode vs openrouter vs codewhale TUI — transport reliability,
context limits, tool access, quoting/encoding quirks are harness properties;
tonight's evidence: gemini CLI web-search 500s, openrouter silently substituting
auto for a pinned model, codex CLI solid across 15 runs). Vary harness where
equivalent lanes exist; record outcomes. (5) BRIDGES OVER BARE APIs (operator, same session): dispatches go through the
managed bridge lanes (dispatch-bridge -> run-*-bridge runners, signals, durable
transcripts) rather than direct API calls — the bridge layer is what makes
outcomes attributable, auditable, and matrix-recordable; generalizes
[[codex-dispatch-must-use-managed-bridge]] to every lane. (6) Distinct-family
review law unchanged — rotation never weakens it, it widens it. See
[[no-mind-is-pigeonholed-to-a-role]].
