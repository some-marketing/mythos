# _dev/commands

The scriptorium annex — command drafts under trial before they join the canonical surface. Draft a command file here, prove it in use, then promote it into .claude/commands/ (and its alias into the registry).
