{
  "schema": "HarnessProtocolPropagation/1.0",
  "task_id": "harness-protocol-parity",
  "mode": "generator-backed-apply-after-g10",
  "g10_gate": "S4 --apply mode must not run before S3 parity validator produces a passing check-only run against the planned propagation output. Apply may only invoke named machine-owned generators, never arbitrary manifest copy actions.",
  "harnesses": ["claude", "codex", "codewhale", "gemini", "opencode", "pi", "hermes"],
  "source_kinds": {
    "hooks": {
      "canonical_sources": [
        "tools/kernel/hooks",
        ".claude/hooks"
      ],
      "fanout": {
        "claude": { "rule": "native", "targets": [".claude/settings.json", ".claude/hooks"] },
        "codex": { "rule": "emulation", "targets": ["tools/codex/lib/hook-emulation.js", "tools/codex/hook-emulator.js"] },
        "codewhale": { "rule": "not_applicable", "reason": "No .claude/settings.json hook surface in Codewhale; native approval/permission gate is the enforcement boundary (see instructions/adapters/codewhale.yaml lifecycle_events)." },
        "pi": { "rule": "bridge", "targets": [".pi/extensions/sm-os-hooks.ts"] },
        "gemini": { "rule": "not_applicable", "reason": "No local lifecycle hook surface found; record capability-gap or instruction-only." },
        "opencode": { "rule": "not_applicable", "reason": "No OpenCode lifecycle hook surface exists in this checkout; use instruction/rule surfaces only." },
        "hermes": { "rule": "not_applicable", "reason": "No Hermes lifecycle adapter found; current surface is ported skills only." }
      }
    },
    "skills": {
      "canonical_sources": [
        ".claude/skills"
      ],
      "fanout": {
        "claude": { "rule": "native", "targets": [".claude/skills"] },
        "codex": { "rule": "not_applicable", "reason": "Codex skills are installed outside this repo; do not infer local fanout from repo state." },
        "codewhale": { "rule": "native", "targets": [".claude/skills"], "note": "Codewhale loads .claude/skills as its skill surface (verified 2026-08-03)." },
        "pi": { "rule": "port", "targets": [".pi/skills/sm-os"] },
        "hermes": { "rule": "port", "targets": [".hermes/skills/sm-os"] },
        "gemini": { "rule": "instruction", "targets": ["GEMINI.md", "instructions/adapters/gemini.yaml"] },
        "opencode": { "rule": "generate_machine_owned", "targets": ["OPENCODE.md", ".opencode/rules/sm_os.md"], "note": "Generated by tools/instructions/lib/engine.js through harness-sync-crawler." }
      }
    },
    "commands": {
      "canonical_sources": [
        "instructions/canonical/commands",
        ".claude/commands",
        "tools/codex/commands"
      ],
      "fanout": {
        "claude": { "rule": "native", "targets": [".claude/commands"] },
        "codex": { "rule": "managed", "targets": ["AGENTS.md", "tools/codex/commands"] },
        "codewhale": { "rule": "managed", "targets": ["AGENTS.md"], "note": "Codewhale runs Mythos commands via tools/commands/mythos-command-runner.cjs and validates against the AGENTS.md managed list (target-command-policy.cjs MANAGED_COMMAND_TARGETS)." },
        "opencode": { "rule": "instruction-managed", "targets": ["OPENCODE.md", ".opencode/rules/sm_os.md"], "note": "No .opencode/commands surface is claimed; target-command-policy fallback to .claude/commands remains an execution policy, not a generated OpenCode command registry." },
        "gemini": { "rule": "freeform", "targets": ["GEMINI.md"], "note": "Arbitrary slash commands must not be claimed for Gemini." },
        "pi": { "rule": "skill-port", "targets": [".pi/skills/sm-os"], "note": "Pi is not registered as a dispatch target." },
        "hermes": { "rule": "skill-port", "targets": [".hermes/skills/sm-os"], "note": "Hermes is not registered as a dispatch target." }
      }
    },
    "framework_harnesses": {
      "canonical_sources": [
        "frameworks"
      ],
      "fanout": {
        "claude": { "rule": "generate", "targets": ["frameworks/*/*/.claude", "tools/workspace/generate-harness.js"] },
        "codex": { "rule": "instruction-preview", "targets": ["AGENTS.md", "instructions/generated"] },
        "codewhale": { "rule": "instruction-preview", "targets": ["AGENTS.md"], "note": "Codewhale consumes the AGENTS.md root-instructions surface (same as codex); no separate generated preview tree claimed." },
        "gemini": { "rule": "instruction-preview", "targets": ["GEMINI.md", "instructions/generated"] },
        "opencode": { "rule": "generate_machine_owned", "targets": ["OPENCODE.md", ".opencode/rules/sm_os.md", "instructions/generated"] },
        "pi": { "rule": "skill-or-command-port", "targets": [".pi/skills/sm-os"] },
        "hermes": { "rule": "skill-or-command-port", "targets": [".hermes/skills/sm-os"] }
      }
    }
  }
}
