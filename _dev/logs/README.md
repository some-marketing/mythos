# `logs/`

Append-only machine logs, most notably `archive.jsonl` — one line per archive or
signal-close operation, per `../policies/data-handling.md`. Plain gloss: your
append-only event-log directory, distinct from the narrative reports in `reports/`.
