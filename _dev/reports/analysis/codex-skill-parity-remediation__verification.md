# Codex skill parity remediation — coordinator verification

Refreshed under the approved parity amendment at `2026-09-14T18:51:57Z` in the declared isolated worktree on `codex/skill-parity-remediation`.

## Mechanical results

- Combined projector, lifecycle, and alias regressions: 74 passed, 0 failed.
- `node tools/skills/sync-codex-skills.cjs --check`: aligned.
- `node tools/skills/sync-lifecycle-command-skills.cjs --check`: six lifecycle skills aligned against the committed general projection evidence.
- Pinned Codex `quick_validate.py`, using the repository PyYAML environment: 131 of 131 applied packages valid; 176 of 176 top-level staged candidate packages valid. Nested child skills are excluded from parent resources, so the staged tree contains exactly 176 independently projected `SKILL.md` files.
- Private-path and obvious-secret-pattern scan over applied and staged candidate trees: no matches.
- `npm run instructions:generate`: completed and wrote the generator-owned targets.
- `npm run instructions:validate`: passed with no parity or drift errors.
- `git diff --check`: passed.
- Scoped status check: zero changes under `instructions/canonical/**` and zero under `clients/**`.

## Protected parity amendment

- Wiring graph regenerated: 4,091 nodes, 2,900 edges, 0 unresolved.
- Target-only baseline repair registered 3,361 expected files while preserving the complete source and membrane policy objects byte-for-byte.
- Portable `npm run verify:parity`: `ok: true`, 0 findings.
- Authoritative private-denylist verification against the existing bound hash: `ok: true`, 0 findings. The external file and its location are not serialized in tracked artifacts.
- Parity test suite: 28 passed, 0 failed.

The first package-validation attempt used a Python interpreter without PyYAML and failed before parsing any package (`ModuleNotFoundError: yaml`). It was rerun with the repository's dependency environment, which carries PyYAML 6.0.3, and all packages passed.

## Projection truth

- 130 canonical command skills applied: 125 `ADVISORY`, 5 handler-backed `BLOCKING`. Eight typed aliases with their own canonical workflows retain distinct runtime pointers; handler capability is derived from each wrapper's resolved execution target.
- 1 direct project skill applied: `go`.
- 2 direct project skills blocked `ABSENT / missing_source`: `meditate` and `outward-inward-loop`; `ticktock` is additionally blocked `ABSENT / dependency_unavailable` because its mandatory TOCK phase requires `meditate`.
- 9 aliases attached as target metadata: 8 `ADVISORY`, 1 handler-backed `BLOCKING`; `/tt` is blocked `ABSENT / target_unavailable`, and no alias body was generated.
- 45 framework helpers staged `ADVISORY / pending_review`; none applied.
- Lifecycle applications now merge applied and already-aligned target custody into the canonical general ledger and bind the updated ledger hash into the general projection index. Candidate-only staging validates every candidate identifier and staged output path before it can clear prior evidence.
- Private-home detection covers Unix paths plus Windows drive and UNC forms with either separator style. Lifecycle application validates canonical custody before target writes, lifecycle checks scope installed-package and orphan drift to the lifecycle family while validating the shared evidence tree, and nested non-directory resource parents are preserved as reported drift.
- Duplicate canonical basenames retain source-distinct blocked collision receipts instead of silently replacing one another. Blocked candidates classify non-directory package roots as drift, and orphan checks inspect the managed package root so residual resources remain visible after the primary `SKILL.md` disappears.
- Private-home detection accepts a token boundary as well as a child-path separator after the username. Blocked candidates inspect their full package roots for residual resources, and direct-skill dependency gating resolves transitive unavailability to a fixed point independent of declaration order.
- Bundled-resource relative paths are scanned for private and credential material before their names enter evidence or staged packages. Receipt checks normalize only declared environment-dependent application states and reject unknown or candidate-incompatible claims. The PR parity workflow runs the projector checks and focused projector/lifecycle/alias regressions through `npm run verify:codex-skills`.
- Automatically discovered framework source paths are scanned before their identities enter rendered candidates or receipts, with redacted failure diagnostics. Frontmatter parsing normalizes CRLF and lone carriage returns before delimiter and field parsing.
- Linux root-home paths are included in private-path rejection. Nested canonical command files are retained only as blocked evidence because runtime authority resolves at the top-level command directory. Stale blocked-target drift is reported only for targets present in projector custody, preserving unrelated user-owned packages.
- Credential detection covers high-confidence environment-variable assignments and AWS temporary access-key identifiers. Direct and framework normalization preserve declared `execution_mode` and `trust_tier` boundaries under Codex-supported `metadata`; the managed `go` projection and its staged evidence are reconciled to that output.
- Credential-assignment detection consumes optional quote delimiters before evaluating values. Normalization also preserves supported scalar `license`, `compatibility`, and `allowed-tools` restrictions. Lifecycle compatibility checks compare only lifecycle staged candidates and receipts while allowing unrelated evidence in the shared canonical tree.
- Credential-assignment detection accepts both shell-style equals and YAML-style colon delimiters without retaining matched values.
- Credential-assignment detection also accepts optional quote delimiters around bounded credential keys, covering JSON and TOML forms.
- Credential-assignment detection recognizes generic `api_key`, `api-key`, and `apiKey` fields, and supported `allowed-tools` metadata accepts YAML sequence values.
- Common client-secret, password, access-token, refresh-token, and secret-key fields enter the same redacted rejection path. Canonical-command and framework discovery roots are validated before recursive traversal.
- Private-key and authentication-token assignments are rejected before staging. Credential-shaped canonical source paths fail with sanitized diagnostics before candidate IDs or receipts are constructed.
- Generic private-key PEM headers, including encrypted PKCS#8 blocks, are rejected. Frontmatter requires scalar names and descriptions, and canonical command projections exceeding Codex's 64-character skill-name limit remain blocked.
- Handler registries require contained, regular, non-symlink repository files before module loading. Inline YAML comments are stripped outside quotes, lifecycle selection uses stable receipt targets after collision suffixing, and shell-variable credential references remain allowed.
- 189 evidence receipts emitted; a separately hashed ledger preserves custody of 131 applied targets across candidate-only staging and is reconciled with valid prior application receipts before disposable evidence is refreshed. `--check` verifies the staged candidate tree, semantic receipt details, projection index, and custody ledger as well as installed packages; ordinary staging can regenerate malformed disposable receipts without weakening the strict custody ledger. Collision rejection covers every real package candidate, including candidates already blocked for another reason, while intentional alias metadata target-sharing remains exempt. Bundled-resource packages carry deterministic path/hash/mode manifests and complete-package hashes. Primary sources and resources must be regular non-symlink files contained by repository-anchored, non-symlink declared source roots before reading; candidate and target roots reject symlink ancestry, target roots receive repository-containment checks before writes, and every selected destination is preflighted before application begins. Absolute home paths are rejected regardless of assignment delimiters. Sensitive resource names, high-confidence credential material including GitHub, AWS, Slack, GitLab, and Google token families, and rendered canonical content containing protected material are rejected without retaining their bytes. Cleanup validates its complete generated-artifact set before deletion. Executable modes are preserved on newly written resources and checked thereafter, while obsolete, non-file, or non-directory installed package paths are reported as drift and remain untouched. No collisions and no unclassified `UNKNOWN` projection was applied.

## Live Codex result

Fresh `codex exec --ephemeral --sandbox read-only` confirmed that `go` and `source-command-ground-in-philosophy` are discoverable with the expected authority boundaries. It reported `ticktock`, `/tt`, and the staged framework helper `guild-wordpress-qa-qa-rerun-verify` as unavailable; the negative results were corroborated against the applied catalog and current receipts because the runtime warned that its model-visible skill list exceeded the context budget. Receipt: `codex-skill-projections/runtime-receipt-codex-discovery.md`.

## Distinct review

Claude and Gemini reviewed the implementation and the protected parity amendment. The first Cloud Codex PR review led to candidate-path hardening, typed workflow preservation, resource reconciliation, lifecycle staging isolation, and correction of the instruction alias loader/renderer. The second Cloud Codex review identified two further path-identity gaps: unvalidated candidate IDs and filename/internal canonical-ID conflation. Both are repaired with slug validation, filename-keyed command loading, mismatch rejection, contained-write checks, and regressions. The third Cloud Codex review found that `ticktock` remained exposed despite its mandatory `meditate` dependency being absent; dependency gating now keeps both `ticktock` and `/tt` unavailable. The fourth Cloud Codex review found literal blocking-command argument placeholders and nested child skills bundled as parent resources; blocking projections now require the actual invocation arguments, while nested skills project only through their own receipts. The fifth Cloud Codex review found private rejected bytes still entering staging, raw malformed lines entering receipts, and incomplete resource hashing; rejected packages now retain only sanitized receipt metadata, parse errors expose line numbers only, and package evidence covers every resource path and hash. The sixth Cloud Codex review found symlinked-resource escape and executable-mode loss; resource collection now rejects symlinks before reading, records modes, preserves modes on new files, and detects later mode drift. The seventh Cloud Codex review found the equivalent symlink gap on primary skill sources; canonical, direct, and framework primary files now require non-symlink regular-file identity and resolved containment before reading. The eighth Cloud Codex review found credential-bearing regular resources and symlinked source-root ancestry; sensitive filenames and high-confidence credential patterns are now rejected, and declared source roots are anchored inside the repository with every ancestor checked for symlinks. The ninth Cloud Codex review found dangling destination symlinks, conflicting terminal advertisement for typed wrappers, and advisory classification of handler-backed wrappers; destination components are now lstat-checked immediately before writes, typed wrappers own their discovery metadata, and their execution tier follows the resolved handler. The tenth Cloud Codex review found stale installed targets omitted when their candidates become blocked; `--check` now reports those preserved obsolete installations as drift instead of returning aligned. The eleventh Cloud Codex review found orphaned installed targets when a source candidate disappears entirely; receipt-backed custody tracking now detects those managed orphans without scanning or claiming unrelated user-owned skills. The twelfth Cloud Codex review found a dangling staging-index symlink escape, raw JSON parser diagnostics entering receipts, and collision evidence being overwritten by shared candidate identifiers. Staging now rejects symlinked generated artifacts before cleanup or writes, canonical parse failures emit location-only diagnostics, and colliding projections receive deterministic evidence identifiers while retaining their shared blocked target identity. The thirteenth Cloud Codex review found that candidate-only staging discarded orphan-detection history and that removed bundled resources remained installed without drift. Applied-target custody now lives in a durable generated ledger that survives staging refreshes, and package alignment now treats unexpected installed files as preserved conflicts. The fourteenth Cloud Codex review found that invalid canonical IDs still leaked their raw values into receipts and that the runtime receipt predated dependency gating. Canonical-ID diagnostics now expose only the filename-derived identity and failure category, and a fresh read-only runtime probe records `ticktock` and `/tt` as unavailable. The fifteenth Cloud Codex review found that rendered canonical descriptions and overrides were not scanned, a symlinked target root could redirect writes outside the repository, and non-file installed targets aborted drift checks. Canonical output now passes the same private/credential scan and redacted rejection path, target-root containment is validated before any write, and package alignment classifies non-regular installed paths as preserved drift. The sixteenth Cloud Codex review found that check mode ignored missing or stale staging evidence and that malformed disposable receipts prevented regeneration. Check mode now compares the complete staged candidate/receipt/index evidence surface, while candidate-only staging repairs corrupt receipts using the independently persisted custody ledger. The seventeenth Cloud Codex review found missing Slack/GitLab/Google credential patterns, non-atomic generated cleanup validation, partial application before a later unsafe destination, and non-directory package-root aborts. The credential detector now covers those high-confidence formats, cleanup preflights every managed artifact before deleting any, application precomputes alignment for every selected package before the first write, and package-root corruption becomes preserved drift. The eighteenth Cloud Codex review found that already-blocked package candidates escaped collision detection, semantic rejection details were normalized out of check mode, and the managed-target ledger itself was not checked. All non-alias package candidates now participate in collision rejection, deterministic details remain part of receipt equality, and the projection index binds the strict custody ledger while applied receipts must be represented in it. The nineteenth Cloud Codex review found candidate-root symlink ancestry, assignment-delimited home paths, and ledger entries dropped before application receipts were cleared. Candidate roots now reject symlink ancestry before cleanup, absolute home paths are detected without delimiter assumptions, and valid application receipts are always reconciled into the ledger before disposable evidence is regenerated. The twentieth Cloud Codex review found that the lifecycle compatibility check still expected an uncommitted lifecycle-only evidence tree. Its default check path now delegates to the committed general projector evidence, while lifecycle-only staging and application remain isolated. Gemini approved the earlier repaired tree. A remaining Claude block was tested directly and adjudicated: legacy aliases render correctly, and resolved-root plus strict-descendant checks satisfy the declared local CLI threat model. Final review synthesis: `convene-runs/20260914T185255Z-codex-skill-parity-final-code-review/synthesis.md`.

The twenty-first Cloud Codex review found that lifecycle applications did not merge their custody into the canonical ledger and that an invalid generated candidate identifier could fail only after prior evidence was cleared. Lifecycle application now merges custody into the canonical ledger and rebinds its index hash, while staging preflights every candidate identifier and output path before cleanup; both cases have regressions. The twenty-second Cloud Codex review found four remaining edges: Windows home paths were not rejected, lifecycle custody was validated only after package writes, nested non-directory resource parents aborted alignment, and lifecycle checks included unrelated installed-package drift. All four are repaired and covered by focused regressions. The twenty-third Cloud Codex review found silent replacement of duplicate canonical basenames, an unclassified `ENOTDIR` path for blocked candidates, and orphan packages missed when only residual resources remained. The loader now retains both duplicate sources as source-distinct blocked collision evidence, blocked target checks classify `ENOTDIR` as drift, and orphan custody checks inspect the full managed package root; each repair has a focused regression. The twenty-fourth Cloud Codex review found terminal home paths without child separators, residual package resources for still-present blocked candidates, and order-sensitive transitive dependency gating. Home detection now accepts token boundaries, blocked candidates inspect package roots, and dependency gating iterates to a fixed point; all three are covered by focused regressions. The twenty-fifth Cloud Codex review found credential-bearing resource names, unchecked receipt application-status claims, and absent CI enforcement for projector drift. Resource paths now pass the private/credential scan with redacted rejection diagnostics, receipt comparison validates a bounded status class, and the PR workflow runs the complete projector gate; the two mechanism changes have focused regressions and the CI command passes locally. The twenty-sixth Cloud Codex review found credential-bearing framework source paths and CRLF-sensitive frontmatter parsing. Framework paths now fail through a redacted pre-staging gate, and frontmatter is line-ending-normalized before parsing; both repairs have focused regressions. The twenty-seventh Cloud Codex review found unrecognized Linux root-home paths, unique nested canonical commands with unresolvable top-level runtime pointers, and drift claims over unmanaged blocked-candidate targets. Root-home paths now reject, nested command files block with explicit evidence, and blocked-target drift requires canonical custody; all three have focused regressions. The twenty-eighth Cloud Codex review found credential assignments/AWS temporary keys and discarded direct-skill execution constraints. Credential scanning now detects assignment forms and `ASIA` keys, while normalization carries execution mode and trust tier under supported metadata; both repairs have focused regressions and the managed `go` package is reconciled. The twenty-ninth Cloud Codex review found quoted credential assignments, dropped `allowed-tools`, and unrelated staged-evidence drift in lifecycle checks. Optional quote delimiters are now consumed by the assignment detector, supported scalar restrictions are preserved, and lifecycle evidence comparison is subset-scoped against the shared tree; the focused suite covers all three. The thirtieth Cloud Codex review found YAML-style colon-delimited credential assignments. The bounded assignment detector now accepts both colon and equals delimiters, covered by the existing redaction regression. The thirty-first Cloud Codex review found quoted credential keys. The same detector now accepts optional key quotes, covered by the existing assignment redaction regression. The thirty-second Cloud Codex review found generic API-key fields and multiline `allowed-tools` YAML sequences. Generic API-key names now enter the same redacted rejection path, and the frontmatter parser consumes sequence values into a deterministic flow-list projection; both have focused regressions. The thirty-third Cloud Codex review found additional common secret fields and pre-validation traversal of canonical/framework roots. The detector now rejects those bounded secret-field assignments without retaining their values, and both discovery roots are validated before `walk()`; regressions cover empty external symlink targets. The thirty-fourth Cloud Codex review found credential-shaped canonical paths leaking through receipt identifiers and missing private-key/auth-token assignment names. Canonical paths now fail through a sanitized pre-identifier gate, and both field families enter the redacted assignment detector; focused regressions verify no candidate or receipt staging. The thirty-fifth Cloud Codex review found encrypted PEM headers, sequence-valued descriptions that reached string renderers, and overlong canonical projection names. Generic PEM detection, scalar frontmatter validation, and a 64-character canonical projection-name gate now block those cases with focused regressions. The thirty-sixth Cloud Codex review found unconfined handler loading, inline-comment corruption of execution metadata, collision-suffixed lifecycle evidence exclusion, and false positives for shell-variable credential references. Handler loading now passes repository source validation before `require()`, YAML comments are parsed outside quoted scalars, lifecycle membership uses stable receipt targets, and `$NAME`/`${NAME}` references remain non-secret; focused regressions cover all four.

## Falsifiers and residuals

Acceptance would be disproved by an applied `UNKNOWN` or pending candidate, copied alias behavior, a non-handler command marked `BLOCKING`, a private path in a shipped projection, a canonical/client diff, a failed runtime discovery claim, or an unresolved distinct-family blocker. None was observed.

Residuals are the two missing direct sources, the dependency-blocked `ticktock` flow, 45 unreviewed framework helpers, non-append-only first-write evidence, and broader foreign-extra coverage in `--check`. These remain explicitly outside the accepted capability claim. The instruction alias-renderer mismatch is repaired and covered by focused tests. Repository parity is both portable-verified and privately ratified for this amended tree.
