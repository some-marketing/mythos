# Codex skill parity remediation — implementation review synthesis

- Produced by: `codex`
- Produced at: `2026-09-14T17:24:22Z`
- Reviewers: `claude` and `gemini`
- Final verdict: **APPROVED**

Both distinct-family reviewers agreed that the source-family projector, additive custody boundary, capability tiers, alias metadata strategy, and review-pending framework staging satisfy the reviewed plan. Claude's required finding was repaired by replacing the absolute home-directory link in the runtime receipt with the repository-relative authority path.

The initial reviewer split over the generator-produced `/9 -> /undefined` line was resolved in the focused run at `../20260914T172316Z-codex-skill-parity-remediation-derived-output-ruling/`. Both reviewers approved keeping the line. It is the unmodified generator's current output, the plan explicitly owns generator-derived outputs, and reverting only that line would leave `npm run instructions:validate` red. The underlying alias-renderer schema mismatch is recorded as out-of-scope debt, not repaired here.

Accepted residuals:

- `meditate` and `outward-inward-loop` remain `ABSENT / missing_source` because their accepted Claude sources are not present on the branch base.
- Forty-five framework helpers remain `ADVISORY / pending_review` candidates and are intentionally absent from Codex runtime discovery.
- First-write custody receipts are not append-only; the current regenerated receipts say `already_aligned`. This must be hardened before the projector becomes a blocking parity gate.
- `--check` covers reviewed-safe projected candidates, not review-pending helpers or foreign extras, so its result must continue to be reported beside the separate counts.
- The instruction alias renderer currently emits numeric `/N -> /undefined` rows because its expected alias shape does not match `SMOSCommandAliasRegistry/1.0`. Fixing `tools/instructions/` requires a separate plan.

Bound evidence hashes:

- Runtime receipt: `09f17f79be3361b78ab6b90de5ad60a8646a02e7e8383574fc38debe0f8c0c94`
- Projector: `b623713b1003932d69f8f035d184dccfd64b1d2872fb516de747e1ea1d718566`
- Projector tests: `d6ea98ffba35df3dc3657a81eb666c5d6235c87c76974fc2d8cfd96e4f00f349`
- Codex adapter: `4e1bd036130be5d0925147e4022967a8289f51ed38991789033d21ca33237dd8`
