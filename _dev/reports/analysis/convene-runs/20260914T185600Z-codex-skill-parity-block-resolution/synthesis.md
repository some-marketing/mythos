# Synthesis: Claude Block Resolution

Claude retained a `BLOCK` after the resolved-path hardening. Its legacy-alias claim was contradicted by the implementation and a direct executable falsifier: a record with `resolves_to` and no typed `target` correctly renders through the legacy path with no undefined value. Its remaining cleanup concern required an unproduced concurrent local symlink-swap attack after the new resolved-root and strict-descendant checks.

Because the stated blockers conflicted with current executable evidence, the result was routed to a distinct-provider adjudication rather than accepted or dismissed by the producer.
