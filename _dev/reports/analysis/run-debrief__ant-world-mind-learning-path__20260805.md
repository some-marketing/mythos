# Run debrief — ant-world-mind-learning-path (COMPLETE, G-MIND-LEARNING-REVIEW cleared)

> 2026-08-05T04:38Z · `/go` orchestration · Producers: opus workers ×3 (memo, S1/S2, S1b)
> Trials: codex GPT-5.5 ×4 (plan r1, design gate, S3, final ruling 20260805T043204Z)

## The result

**The world-mind learns.** One-step masked prediction error on a hidden-layer head —
the only signal that cannot bribe the mirror gate (the mind's own seed verbs author
87% of the detector's features; outcome rewards are structurally confounded).
Execution-verified: prediction loss improves 27×/2,318× (n=2 seeds);
byte-deterministic (same seed → identical weights and all 600 decision rows);
**mid-learning checkpoint round-trip byte-identical** — a mind saved mid-thought and
resumed continues exactly (the convene's continuity-of-learning wish, literal);
frozen control at exactly 0; contamination alarm quiet (verb weights provably
untouched); confound test discriminates with the D1 applicability predicate.

## The final ruling (codex 20260805T043204Z)

- **(b″) PASS including the checkpointed prediction head: 0.0645/0.0582 > 0.05.**
  Trunk-only raw measurement (0.0147/0.0097 FAIL) preserved verbatim. Lesson
  recorded: metrics over auxiliary checkpointed state must name their parameter set.
- **Coord 7 (starvation) = nonblocking world-statistic precondition**, not wiring:
  the coupling repair is real (coord 4 carries ~28% of the loss; the dead-zero bug
  class has a firing falsifier), but no hive currently starves, so the coordinate is
  honestly zero. The goal round's survival pressure will make it live — the ecology
  and the curriculum converge on the same next event.
- Slice committable with the starvation-pressure follow-up recorded.

## Chain of discoveries this arc (each found by the process, not despite it)

1. Inert weights (shape bug) → repaired, invalidation proven with pre-fix control.
2. Thinks-but-cannot-learn (no update path) → world-train.js, this slice.
3. Dead loss coordinates (encoder/state decoupling, same bug family) → S1b repair
   with detectability assertion.
4. The specification boundary (which parameters count) → ruled, with raw record.

## Follow-ups (named, nonblocking)

- Starvation-pressure precondition: coord 7 goes live with the goal round
  (operator decision, outside all pre-stamps).
- CheckpointManifest/1.1 shipped with 1.0 fallback (five-probe contract PASS);
  world-state 1.1.0 additive schema (dashboard + importer verified tolerant).
- Deploying the LEARNING mind to Orwell = fresh operator decision.

## Verification posture

Twelve verdicts (ten retained + two S1b additions), all with named evidence in
`_dev/state/mind-learning-test/world-mind-learning-evidence.json` (169KB); continuity
refusal ladder re-PASS 10/10 after every engine change; nothing tuned to pass — the
learning rate stayed contract-hashed through two FAIL rounds, and the bar moved only
by distinct-family ruling with the raw numbers preserved.
