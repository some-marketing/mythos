# Run debrief — Codex skill parity remediation

## Improve

- Preserve the four-family projector architecture and keep capability reporting separated into presence, package validity, semantic review, runtime discovery, and deterministic handler backing.
- Add an append-only, run-stamped application ledger before promoting the projector to a blocking parity gate.
- Give `--check` an explicit coverage report for pending candidates and foreign extras rather than expanding the meaning of its current green result.
- Plan a bounded `tools/instructions/` repair for the alias schema mismatch that emits numeric `/N -> /undefined` rows.
- Review framework helpers per framework with their manifests and guardrails open; do not bulk-promote the current 45 candidates.

## Replicate

- Use an isolated worktree whenever the shared checkout has ambiguous or foreign custody.
- Derive deterministic capability from the exported runtime registry, not configuration membership or prose.
- Stage source-family transformations with source hashes and semantic state before application.
- Treat live runtime absence as `UNKNOWN`, even when a syntactically valid candidate exists on disk.
- Use distinct-family review to resolve both architecture and closeout disputes, and bind the synthesis to current artifact hashes.

## Honest closeout

The mechanism and reviewed-safe surface are complete. Full harness parity is not: two direct skills lack accepted source on the branch base, and 45 framework helpers await semantic review. No existing ambiguous-custody package was overwritten.
