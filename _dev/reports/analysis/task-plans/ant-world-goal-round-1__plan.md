# Task Plan — ant-world-goal-round-1

> Operator: "we can do both. do the continuity AND the food and water pressure"
> (2026-08-05T04:41Z — recorded as the conditional learning-mind deployment stamp)
> Scope: system · Risk: high · Review lane: operator-gate (conditional stamp recorded)

## What

The program's first real experiment: resume-chained 150-tick turns on Orwell with
the LEARNING mind under a food/water survival-pressure goal packet — an
independently evaluable predicate with a deadline, thresholds DERIVED from the
continuity-control and baseline data. The expected convergence, tested not assumed:
genuine scarcity brings encoder coord 7 (starvation) alive, completing the mind's
curriculum while addressing the ecology's crisis.

## Steps

- **S0** — Goal packet design: `GoalPacket/1.0` + a read-only, ecology-only
  evaluator (never build/feature coordinates — the goal must not bribe the mirror);
  every threshold cites the observed datum it derives from.
- **S1** — Local rehearsal: 3 resume-chained goal turns in the sandbox; evaluator
  honesty, coord-7 activation measured, learning-across-resumes, alarm quiet,
  confound clean.
- **S2** — Codex trial (**G-GOAL-ROUND-REVIEW**) → deployment packet under the
  recorded conditional stamp.
- **S3** — Run on Orwell; per-turn harvests → dashboard + Unreal; science readout
  (goal progress, coord-7, loss trajectory, mirror-p with confound checks); debrief
  → **operator text (improved/learned/next)** → triggers the first `/meditate`.

## Amendment r1 (2026-08-05T04:56Z — codex 20260805T044843Z, CHANGES_REQUIRED, all six applied)

- **F1, the key ruling — one-way evaluator isolation**: the evaluator runs in the
  post-tick REPORTING path only; nothing in the mind/hive/train call graphs reads
  its output (call-graph audit in S1 evidence). Hunger reaches behavior through the
  world the mind already perceives; the goal's *evaluation* never does. That line
  is what makes hunger-built structure emergence, not confound.
- **F2**: every threshold = {value, units, formula, source_field, aggregation,
  derivation_datum} + rehearsal reachability test; packet + evaluator hashed.
- **F3**: stamp conditions completed (S1 PASS prerequisite, pinned hashes,
  passthrough validation, isolation evidence, deterministic extinction rule:
  extinction runs to deadline unmodified and is recorded as data).
- **F4**: rehearsal gains a same-seed no-goal CONTROL arm (the behavioral delta IS
  the goal's effect), evaluator per-tick trace, tamper probe, and the
  GoalRoundRehearsalEvidence/1.0 schema.
- **F5**: coord-7 activation operationalized (encoder's own stockpile≤0 predicate,
  ≥1 tick; ecology measurement, explicitly not a learning claim).
- **F6**: scope_identity.owned_artifacts added.

## Bounds

Halt-for-repair unchanged; membrane unchanged; hive extinction is a recorded
outcome, not prevented by evaluator design; any confound flag or resume refusal
halts the round.
