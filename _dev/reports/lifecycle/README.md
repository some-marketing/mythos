# `reports/lifecycle/`

Traces of session and hook lifecycle events — session starts, tool-use hooks, boundary
crossings. Plain gloss: your event-log directory for harness-level lifecycle machinery,
as distinct from the work findings that live in `../analysis/`.

Useful for reconstructing "what actually happened during this session" independent of
what any actor later claims happened.
