# `state/motivation/`

State for periodic self-check scans — the workshop's own equivalent of "is this still
worth doing." Plain gloss: scheduled check-in state, kept separate from task-execution
state so a stalled check-in never blocks real work.
