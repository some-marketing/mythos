# Replay case notes

This synthetic case tests whether the candidate can distinguish observed needs from assumptions and produce a useful product definition without drifting into architecture or implementation.

A structural preflight is not a true replay. The four prompts must still be executed, independently reviewed, and compared with the existing blueprint/plan path.
