{
  "type": "array",
  "minItems": 1,
  "items": {
    "type": "object",
    "required": [
      "baseline_requirement_id",
      "behavior",
      "source_locator",
      "authority",
      "consumers"
    ],
    "properties": {
      "baseline_requirement_id": { "type": "string", "minLength": 1, "pattern": "\\S" },
      "behavior": { "type": "string", "minLength": 1, "pattern": "\\S" },
      "source_locator": { "type": "string", "minLength": 1, "pattern": "\\S" },
      "authority": { "type": "string", "minLength": 1, "pattern": "\\S" },
      "consumers": { "type": "array", "items": { "type": "string" } }
    }
  }
}
