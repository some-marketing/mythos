{
  "type": "object",
  "required": [
    "added",
    "modified",
    "removed",
    "preserved_invariants"
  ],
  "anyOf": [
    { "properties": { "added": { "minItems": 1 } } },
    { "properties": { "modified": { "minItems": 1 } } },
    { "properties": { "removed": { "minItems": 1 } } }
  ],
  "properties": {
    "added": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["requirement_id", "requirement", "scenarios"],
        "properties": {
          "requirement_id": { "type": "string", "minLength": 1, "pattern": "\\S" },
          "requirement": { "type": "string", "minLength": 1, "pattern": "\\S" },
          "scenarios": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1, "pattern": "\\S" } }
        }
      }
    },
    "modified": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["requirement_id", "baseline_requirement_id", "behavioral_difference", "requirement", "scenarios"],
        "properties": {
          "requirement_id": { "type": "string", "minLength": 1, "pattern": "\\S" },
          "baseline_requirement_id": { "type": "string", "minLength": 1, "pattern": "\\S" },
          "behavioral_difference": { "type": "string", "minLength": 1, "pattern": "\\S" },
          "requirement": { "type": "string", "minLength": 1, "pattern": "\\S" },
          "scenarios": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1, "pattern": "\\S" } }
        }
      }
    },
    "removed": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["requirement_id", "baseline_requirement_id", "intended_absence"],
        "properties": {
          "requirement_id": { "type": "string", "minLength": 1, "pattern": "\\S" },
          "baseline_requirement_id": { "type": "string", "minLength": 1, "pattern": "\\S" },
          "intended_absence": { "type": "string", "minLength": 1, "pattern": "\\S" }
        }
      }
    },
    "preserved_invariants": { "type": "array", "items": { "type": "string" } }
  }
}
