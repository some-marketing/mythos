# Baseline behavior

The workflow stores completed document bundles for a fixed 30-day period. Operators cannot choose another duration. Expired bundles are marked inactive but remain visible in the default list. Exported bundles contain the original completion receipt and do not include retention metadata.

The proposed change should add configurable retention, change expired-item visibility, and remove the legacy fixed-duration assumption while preserving export receipts.
