# Baseline behavior

The workflow stores completed document bundles for a fixed 30-day period. Document-workflow users cannot choose another duration. Expired bundles are marked inactive but remain visible in the default list. Exported bundles contain the original completion receipt and do not include retention metadata.
