# Replay case notes

This synthetic case includes one added behavior, one modified constraint, and one removed legacy behavior. It tests whether the candidate preserves the baseline and produces unambiguous deltas without drifting into implementation.

A structural preflight is not a true replay. The five prompts must still be executed, independently reviewed, and compared with the current plan path.
