{
  "id": "fw-media-video-editing",
  "description": "Run Video Editing \u2014 Conversation-driven video editing: transcribe, cut, color grade, burn subtitles, self-evaluate",
  "mode": "COORDINATOR",
  "objective": "Execute the Video Editing framework against a client project. Routes into the per-framework harness at frameworks/media/video-editing/.claude/.",
  "process": [
    "Read authoritative process from .claude/commands/fw-media-video-editing.md and frameworks/media/video-editing/manifest.json.",
    "Verify project path and framework link.",
    "Verify MCP availability per framework manifest.",
    "Execute via /run-framework media/video-editing <project-path>.",
    "Validate outputs against manifest output contract."
  ],
  "success_criteria": [
    "Framework prompt chain executed in correct order",
    "Output artifacts match manifest output contract"
  ],
  "source_of_truth": {
    "claude_command": ".claude/commands/fw-media-video-editing.md",
    "framework_manifest": "frameworks/media/video-editing/manifest.json",
    "canonical_spec_note": "This canonical spec is the thin routing layer. The Claude command (.claude/commands/) and framework manifest carry the full argument contract, prompt chain, phase table, and operational detail."
  }
}
