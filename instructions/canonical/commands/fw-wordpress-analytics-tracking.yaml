{
  "id": "fw-wordpress-analytics-tracking",
  "description": "Run Analytics Tracking \u2014 Analytics implementation framework for WordPress sites",
  "mode": "COORDINATOR",
  "objective": "Execute the Analytics Tracking framework against a client project. Routes into the per-framework harness at frameworks/wordpress/analytics-tracking/.claude/.",
  "process": [
    "Read authoritative process from .claude/commands/fw-wordpress-analytics-tracking.md and frameworks/wordpress/analytics-tracking/manifest.json.",
    "Verify project path and framework link.",
    "Verify MCP availability per framework manifest.",
    "Execute via /run-framework wordpress/analytics-tracking <project-path>.",
    "Validate outputs against manifest output contract."
  ],
  "success_criteria": [
    "Framework prompt chain executed in correct order",
    "Output artifacts match manifest output contract"
  ],
  "source_of_truth": {
    "claude_command": ".claude/commands/fw-wordpress-analytics-tracking.md",
    "framework_manifest": "frameworks/wordpress/analytics-tracking/manifest.json",
    "canonical_spec_note": "This canonical spec is the thin routing layer. The Claude command (.claude/commands/) and framework manifest carry the full argument contract, prompt chain, phase table, and operational detail."
  }
}
