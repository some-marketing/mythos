{
  "id": "fw-wordpress-documentation",
  "description": "Run Documentation \u2014 Client-facing WordPress admin documentation via MCP browser walkthroughs with Notion output",
  "mode": "COORDINATOR",
  "objective": "Execute the Documentation framework against a client project. Routes into the per-framework harness at frameworks/wordpress/documentation/.claude/.",
  "process": [
    "Read authoritative process from .claude/commands/fw-wordpress-documentation.md and frameworks/wordpress/documentation/manifest.json.",
    "Verify project path and framework link.",
    "Verify MCP availability per framework manifest.",
    "Execute via /run-framework wordpress/documentation <project-path>.",
    "Validate outputs against manifest output contract."
  ],
  "success_criteria": [
    "Framework prompt chain executed in correct order",
    "Output artifacts match manifest output contract"
  ],
  "source_of_truth": {
    "claude_command": ".claude/commands/fw-wordpress-documentation.md",
    "framework_manifest": "frameworks/wordpress/documentation/manifest.json",
    "canonical_spec_note": "This canonical spec is the thin routing layer. The Claude command (.claude/commands/) and framework manifest carry the full argument contract, prompt chain, phase table, and operational detail."
  }
}
