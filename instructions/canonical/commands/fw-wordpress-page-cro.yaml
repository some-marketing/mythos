{
  "id": "fw-wordpress-page-cro",
  "description": "Run Page Cro \u2014 Multi-phase conversion rate optimization audit for marketing pages",
  "mode": "COORDINATOR",
  "objective": "Execute the Page Cro framework against a client project. Routes into the per-framework harness at frameworks/wordpress/page-cro/.claude/.",
  "process": [
    "Read authoritative process from .claude/commands/fw-wordpress-page-cro.md and frameworks/wordpress/page-cro/manifest.json.",
    "Verify project path and framework link.",
    "Verify MCP availability per framework manifest.",
    "Execute via /run-framework wordpress/page-cro <project-path>.",
    "Validate outputs against manifest output contract."
  ],
  "success_criteria": [
    "Framework prompt chain executed in correct order",
    "Output artifacts match manifest output contract"
  ],
  "source_of_truth": {
    "claude_command": ".claude/commands/fw-wordpress-page-cro.md",
    "framework_manifest": "frameworks/wordpress/page-cro/manifest.json",
    "canonical_spec_note": "This canonical spec is the thin routing layer. The Claude command (.claude/commands/) and framework manifest carry the full argument contract, prompt chain, phase table, and operational detail."
  }
}
