{
  "id": "fw-wordpress-qa",
  "description": "Run Qa \u2014 Playwright-based site functionality testing with CRM integration validation",
  "mode": "COORDINATOR",
  "objective": "Execute the Qa framework against a client project. Routes into the per-framework harness at frameworks/wordpress/qa/.claude/.",
  "process": [
    "Read authoritative process from .claude/commands/fw-wordpress-qa.md and frameworks/wordpress/qa/manifest.json.",
    "Verify project path and framework link.",
    "Verify MCP availability per framework manifest.",
    "Execute via /run-framework wordpress/qa <project-path>.",
    "Validate outputs against manifest output contract."
  ],
  "success_criteria": [
    "Framework prompt chain executed in correct order",
    "Output artifacts match manifest output contract"
  ],
  "source_of_truth": {
    "claude_command": ".claude/commands/fw-wordpress-qa.md",
    "framework_manifest": "frameworks/wordpress/qa/manifest.json",
    "canonical_spec_note": "This canonical spec is the thin routing layer. The Claude command (.claude/commands/) and framework manifest carry the full argument contract, prompt chain, phase table, and operational detail."
  }
}
