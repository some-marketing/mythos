{
  "id": "oc",
  "description": "Shortest operator alias for council-of-owls: Owl Council",
  "mode": "COORDINATOR",
  "objective": "Provide a two-letter operator shorthand for `/council-of-owls` without creating a second consultation or orchestration contract. OC means Owl Council: consult with `/convene` when council is warranted, then route through `/owl` and canonical `/orchestrate-loop`.",
  "scope_resolution": {
    "default": "Alias to council-of-owls with the same target grammar, council decision, and authority rules",
    "arguments": {
      "<task-or-question>": "Pass through to /council-of-owls target handling",
      "<task-id>": "Pass through to /council-of-owls target handling",
      "<path>": "Pass through to /council-of-owls target handling",
      "active-workstreams": "Pass through to /council-of-owls active-workstreams handling"
    }
  },
  "process": [
    "Treat oc as a human-memory wrapper around `/council-of-owls`, not an independent workflow definition.",
    "Resolve the alias mechanically through `instructions/canonical/command-aliases.yaml`; keep the typed alias as provenance only.",
    "Resolve every invocation through `instructions/canonical/commands/council-of-owls.yaml`.",
    "Preserve the council-of-owls boundary: use `/convene` only when multi-lobe counsel is warranted, then route through `/owl` or canonical `/orchestrate-loop`.",
    "Do not introduce oc-specific routing, state names, artifact contracts, review lanes, actors, or permissions."
  ],
  "routing_rules": [
    "Canonical consult-then-route behavior remains owned by `/council-of-owls`.",
    "Canonical orchestration behavior remains owned by `/orchestrate-loop`, with `/owl` as the readable shorthand.",
    "Use `/oc` when the operator wants the shortest shorthand.",
    "Use `/council-of-owls` in formal specs, tests, and cross-actor handoffs when clarity matters.",
    "Never interpret oc as permission to bypass human-operator gates, destructive confirmations, external publication gates, credential gates, budget/scope commitments, or same-rank authority conflicts."
  ],
  "success_criteria": [
    "Operators can invoke oc with the same target shapes accepted by council-of-owls",
    "The invocation resolves to the same behavior and evidence expectations as council-of-owls",
    "Generated command surfaces list oc as a coordinator alias",
    "No duplicate consultation, orchestration, or closeout contract is introduced"
  ],
  "handoff": {
    "canonical_route": "council-of-owls <target-or-question>",
    "operator_shorthand": "oc <target-or-question>",
    "formal_cross_actor_handoff": "council-of-owls <target-or-question>"
  },
  "artifacts": [
    "instructions/canonical/commands/oc.yaml",
    "instructions/canonical/command-aliases.yaml",
    "instructions/canonical/commands/council-of-owls.yaml",
    "instructions/canonical/commands/owl.yaml",
    "instructions/canonical/commands/orchestrate-loop.yaml",
    ".claude/commands/oc.md",
    ".claude/commands/council-of-owls.md"
  ]
}
