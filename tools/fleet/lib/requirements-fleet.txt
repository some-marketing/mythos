# Minimal deps for tools/fleet/{orchestrator,worker} to come up and register.
# The full simpleminions requirements.txt has heavyweight ML packages (torch,
# onnxruntime, sentence-transformers, faiss-cpu) that are NOT needed for the
# coordinator/worker round-trip. Install those separately on nodes that
# actually run local inference.

# HTTP server (orchestrator + worker daemon)
fastapi>=0.110
uvicorn[standard]>=0.27

# HTTP client (heartbeat, dispatch)
httpx>=0.27

# Data models
pydantic>=2.5

# Hardware introspection (worker hardware.py)
psutil>=5.9
