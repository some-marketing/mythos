# Core ONNX Runtime dependencies
onnxruntime>=1.17.0
onnx>=1.15.0

# Model loading and tokenization
transformers>=4.38.0
torch>=2.2.0

# Utilities
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0

# For tool execution
jsonschema>=4.21.0

# HTTP requests tool
httpx>=0.27.0
aiohttp>=3.9.0

# Retry logic and error handling
tenacity>=8.2.0

# API Server dependencies
# NOTE: Some routers and scripts still rely on Flask primitives. Keep Flask
# installed while the codebase is gradually ported to Quart to avoid
# ModuleNotFoundError when importing modules such as
# image_generation/router.py.
flask>=3.0.0  # Flask (synchronous) - see api_flask_backup.py
flask-cors>=4.0.0
flask-limiter>=3.5.0
gunicorn>=21.2.0

# Quart (async Flask replacement)
quart>=0.19.0
quart-cors>=0.7.0
hypercorn>=0.16.0  # ASGI server (replaces gunicorn)
redis>=5.0.0
flask-limiter>=3.5.0

# Dashboard API dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-multipart>=0.0.6
websockets>=12.0

# Development and testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0  # For code coverage measurement

# ReasoningBank Pattern Learning (M5 Enhancement)
sentence-transformers>=2.2.0  # Task embeddings
aiosqlite>=0.19.0             # Async SQLite for pattern storage
scikit-learn>=1.3.0           # Distance metrics (optional)
faiss-cpu>=1.7.4              # Vector indexing for fast similarity search (Week 5, optional)

# Kubernetes Integration (optional)
# Install via: pip install -r requirements-k8s.txt
# kubernetes>=28.1.0
